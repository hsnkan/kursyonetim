"use client";

import LogoCoin3D from "@/app/components/LogoCoin3D";

/** Kurum logosu — navbar, NFC terminal vb. */
export default function BrandingLogo3D(props) {
  return <LogoCoin3D speed="slow" {...props} />;
}
